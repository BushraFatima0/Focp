//READ TWO INTEGERS AND PRINT THEIR HCF

#include <stdio.h>
int main()
{
    //ques2
    int a,b,hcf;
    printf("Enter two Integers:\n");
    scanf("%d %d",&a,&b);
    for(int i=2; i<=a,i<=b;i++)
    {
        if(a%i==0 && b%i==0)
        hcf=i;
    }
    printf("HCF=%d",hcf);
    return 0;
}
